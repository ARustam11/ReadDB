@model List<ReadDB.Models.YourModel>
<h1>Rustam Ainetdinov 200540293</h1>

<table>
    <thead>
        <tr>
            <th>Id</th>
            <th>Product name</th>
 
        </tr>
    </thead>
    <tbody>
        @foreach (var item in Model)
        {
            <tr>
                <td>@item.pid</td>
                <td>@item.productname</td>
               
            </tr>
        }
    </tbody>
</table>

